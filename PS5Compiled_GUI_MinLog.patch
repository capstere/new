diff -ruN /mnt/data/ps5_gui_min_orig/PS5Compiled/Main.ps1 /mnt/data/ps5_gui_min/PS5Compiled/Main.ps1
--- /mnt/data/ps5_gui_min_orig/PS5Compiled/Main.ps1	2026-01-28 10:40:56.000000000 +0000
+++ /mnt/data/ps5_gui_min/PS5Compiled/Main.ps1	2026-01-28 12:49:49.680579386 +0000
@@ -1466,15 +1466,17 @@
                     $script:RuleEngineShadow = $re
 
                     if ($re -and $re.Summary) {
-                        $pairs = @()
-                        foreach ($k in $re.Summary.ObservedCounts.Keys) { $pairs += ("$k=$($re.Summary.ObservedCounts[$k])") }
-                        if ($pairs.Count -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): ObservedCall counts: " + ($pairs -join ', ')) 'Info' }
+                        if (Test-ConfigFlag $Config 'EnableRuleEngineSummaryLog' $true) {
+                            $pairs = @()
+                            foreach ($k in $re.Summary.ObservedCounts.Keys) { $pairs += ("$k=$($re.Summary.ObservedCounts[$k])") }
+                            if ($pairs.Count -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): ObservedCall counts: " + ($pairs -join ', ')) 'Info' }
 
-                        $dpairs = @()
-                        foreach ($k2 in $re.Summary.DeviationCounts.Keys) { $dpairs += ("$k2=$($re.Summary.DeviationCounts[$k2])") }
-                        if ($dpairs.Count -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): Deviations: " + ($dpairs -join ', ')) 'Info' }
+                            $dpairs = @()
+                            foreach ($k2 in $re.Summary.DeviationCounts.Keys) { $dpairs += ("$k2=$($re.Summary.DeviationCounts[$k2])") }
+                            if ($dpairs.Count -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): Deviations: " + ($dpairs -join ', ')) 'Info' }
 
-                        if ($re.Summary.RetestYes -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): Retest=YES count: " + $re.Summary.RetestYes) 'Info' }
+                            if ($re.Summary.RetestYes -gt 0) { Gui-Log ("🧠 RuleEngine (shadow): Retest=YES count: " + $re.Summary.RetestYes) 'Info' }
+                        }
 
                         if ((Test-ConfigFlag $Config 'EnableShadowCompare' $false) -and $re.TopDeviations) {
                             $n = 0
diff -ruN /mnt/data/ps5_gui_min_orig/PS5Compiled/Modules/Config.ps1 /mnt/data/ps5_gui_min/PS5Compiled/Modules/Config.ps1
--- /mnt/data/ps5_gui_min_orig/PS5Compiled/Modules/Config.ps1	2026-01-28 10:40:56.000000000 +0000
+++ /mnt/data/ps5_gui_min/PS5Compiled/Modules/Config.ps1	2026-01-28 12:49:49.681732572 +0000
@@ -64,6 +64,9 @@
     EnableEquipmentScan  = $false   # Kept for compatibility; ignored when EnableEquipmentSheet is $false
     EnableRuleEngine    = $true       # New RuleEngine (shadow-mode)
     EnableShadowCompare = $true        # Log diff between legacy and rules when EnableRuleEngine is on
+    # When true: writes RuleEngine summary lines to GUI-log (counts/dev/retest).
+    # Set to $false for a quieter GUI-log while still writing RuleEngine_Debug.
+    EnableRuleEngineSummaryLog = $false
     EnableRuleEngineDebugSheet = $true        # Add RuleEngine_Debug worksheet to output (safe, appended)
     RuleEngineDebugIncludeAllRows = $false # When true: write ALL rows in RuleEngine_Debug (legacy behavior)
 
